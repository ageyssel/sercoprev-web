name: Check Supabase privileged secret

on:
  push:
    branches: [ops/verify-production-now]
    paths:
      - .github/workflows/check-supabase-secret.yml

permissions:
  contents: read
  issues: write

jobs:
  check:
    runs-on: ubuntu-24.04
    env:
      GH_TOKEN: ${{ github.token }}
      SUPABASE_SECRET_KEY: ${{ secrets.SUPABASE_SECRET_KEY }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - name: Report secret-name availability only
        shell: bash
        run: |
          secret_key=false
          service_role=false
          [ -n "${SUPABASE_SECRET_KEY:-}" ] && secret_key=true
          [ -n "${SUPABASE_SERVICE_ROLE_KEY:-}" ] && service_role=true

          gh issue comment 6 --repo ageyssel/sercoprev-web --body \
            "### Disponibilidad de credencial privada en GitHub

- \`SUPABASE_SECRET_KEY\`: $secret_key
- \`SUPABASE_SERVICE_ROLE_KEY\`: $service_role

Solo se comprobaron los nombres y su existencia; ningún valor fue leído ni mostrado."
